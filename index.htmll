<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>💖 Important Question 💖</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <style>
    body {
      font-family: 'Poppins', 'Comic Sans MS', cursive;
      background: linear-gradient(135deg, #ffb6c1, #ffe4e1);
      height: 100vh;
      margin: 0;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .card {
      background: white;
      padding: 20px;
      border-radius: 25px;
      text-align: center;
      max-width: 360px;
      box-shadow: 0 15px 30px rgba(0,0,0,0.15);
    }

    h1 {
      color: #ff4d6d;
      margin-bottom: 10px;
    }

    img {
      width: 100%;
      border-radius: 15px;
      margin: 10px 0;
    }

    .buttons {
      margin-top: 15px;
      position: relative;
      height: 70px;
    }

    button {
      padding: 12px 20px;
      font-size: 16px;
      border: none;
      border-radius: 30px;
      cursor: pointer;
    }

    #yes {
      background-color: #ff4d6d;
      color: white;
    }

    #no {
      background-color: #ddd;
      position: absolute;
      left: 160px;
      top: 0;
    }
  </style>
</head>

<body>
  <div class="card">
    <h1>Would you be my boyfriend? 💕</h1>

    <!-- Replace these image links if you want -->
    <img src="IMG_0398.gif" alt="cute gif">
    <img src="IMG_0399.jpeg" alt="funny pic">

    <div class="buttons">
      <button id="yes" onclick="yesClicked()">Yes 💖</button>
      <button id="no" onmouseover="moveNo()">No 🙄</button>
    </div>
  </div>

  <script>
    function moveNo() {
      const noBtn = document.getElementById("no");
      const x = Math.random() * 200;
      const y = Math.random() * 40;
      noBtn.style.left = x + "px";
      noBtn.style.top = y + "px";
    }

    function yesClicked() {
      document.body.innerHTML = `
        <div style="text-align:center;">
          <h1 style="color:#ff4d6d;">YAYYY!! 💕🥰</h1>
          <p>You literally just made my day.</p>
        </div>
      `;
    }
  </script>
</body>
</html>
